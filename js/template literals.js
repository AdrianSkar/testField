let num = 1;
let string = 'string';
let bool = true;

let output = `The num is ${num + 1}, 
the string is ${string} +\n and the bool is ${bool}`;

console.log(output);

function test() {
	return output;
}

// todo: incomplete(marked to pocket: regarding tagged)












// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals

